<div class="hidden md:flex md:w-64 md:flex-col md:fixed md:inset-y-0">
  <!-- Sidebar component, swap this element with another sidebar if you like -->
  <div class="flex flex-col flex-grow pt-5 bg-primary overflow-y-auto">
    <%= link_to root_path, class: "flex items-center flex-shrink-0 px-4" do %>
      <i class="fa-regular fa-hard-drive w-auto h-12 mx-auto text-white"></i>
    <% end %>
    <div class="mt-5 flex-1 flex flex-col">
      <%= render "layouts/site/menu" %>
    </div>
  </div>
</div>
