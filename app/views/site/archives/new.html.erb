<div class="relative z-10" aria-labelledby="modal-title" role="dialog" aria-modal="true">
  <div class="fixed inset-0 bg-gray-500 bg-opacity-75 transition-opacity"></div>
  <div class="fixed inset-0 z-10 overflow-y-auto flex flex-col items-center justify-center p-4 shadow overflow-hidden sm:rounded-md">
    <div class="shadow overflow-y-auto sm:rounded-md bg-white px-4 py-5 bg-white sm:p-6 w-[480px]">
      <h1 class="text-2xl text-gray-600">
        <%= t('.heading') %> /
        <span class="font-semibold">
          <%= t('.heading_register') %>
        </span>
      </h1>
      <%= render('form', archive: @archive) %>
    </div>
  </div>
  <div>
</div>
